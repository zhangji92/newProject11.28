<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@android:style/Theme.NoTitleBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowAnimationStyle">@style/ActivityAnimation</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
    
    <style name="OtherActivityTheme" parent="AppBaseTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/ActivityAnimation</item>
    </style>

    <style name="ActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left_none</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left_none</item>
    </style>

    <!-- calendar -->
    <style name="CalendarTitle">
        <item name="android:textSize">@dimen/calendar_text_medium</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CalendarCell">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <!-- <item name="android:textAlignment">center</item> -->
    </style>

    <style name="CalendarCell.DayHeader">
        <item name="android:textSize">@dimen/calendar_text_small</item>
    </style>

    <style name="CalendarCell.CalendarDate">
        <item name="android:textSize">@dimen/calendar_text_medium</item>
        <item name="android:clickable">true</item>
        <item name="android:textStyle">bold</item>
    </style>
    <!-- calendar -->


    <!-- popupwindow -->
    <style name="PopupAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popup_enter</item>
        <item name="android:windowExitAnimation">@anim/popup_exit</item>
    </style>

    <!-- 更多界面style -->
    <style name="fragment_setting_text_style">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/textversion</item>
    </style>

    <style name="fragment_setting_gray_text_style">
        <item name="android:textColor">@color/net_error_text</item>
        <item name="android:textSize">@dimen/textcode</item>
    </style>
    <!-- xxxx -->
    <style name="navigation_style">
        <item name="android:textColor">@color/important_title</item>
        <item name="android:textSize">@dimen/navigation</item>
    </style>

    <style name="more_feed_back_phone_text_style">
        <item name="android:textColor">@color/more_feed_back_phone_text</item>
        <item name="android:textSize">@dimen/text20</item>
    </style>

    <style name="product_composition_list_item_style">
        <item name="android:textColor">@color/important_title</item>
        <item name="android:textSize">@dimen/product_composition_list_item</item>
    </style>

    <style name="product_composition_name_style">
        <item name="android:textColor">@color/important_title</item>
        <item name="android:textSize">@dimen/product_composition_name</item>
    </style>

    <style name="product_composition_description_style">
        <item name="android:textColor">@color/gray</item>
        <item name="android:textSize">@dimen/product_composition_description</item>
    </style>

    <style name="search_safety_style">
        <item name="android:textColor">@color/use_green</item>
        <item name="android:textSize">@dimen/search_function</item>
    </style>

    <style name="product_composition_list_include_style">
        <item name="android:textColor">@color/important_title</item>
        <item name="android:textSize">@dimen/product_composition_list_include</item>
    </style>

    <!-- 自定义进度条 -->
    <style name="ProgressBar_Mini" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">50dip</item>
        <item name="android:minHeight">8dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:progressDrawable">@drawable/progressbar_mini</item>
    </style>

    <!-- 自定义Dialog -->
    <style name="Custom_Progress" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <!-- <item name="android:windowBackground">@android:color/transparent</item> -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="transparentFrameWindowStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/probation_filter_dialog_choose_bg</item>
    </style>

    <style name="main_menu_animstyle">
        <item name="android:windowEnterAnimation">@anim/probation_filter_dialog_in_anim</item>
        <item name="android:windowExitAnimation">@anim/probation_filter_dialog_out_anim</item>
    </style>

    <style name="roomRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_bar_bg</item>
        <item name="android:minHeight">35dp</item>
        <item name="android:maxHeight">35dp</item>
    </style>
    
    <style name="brandRatingBar">
        <item name="android:progressDrawable">@drawable/brand_ratingbar_bg</item>
        <item name="android:thumb">@null</item>
        <item name="android:isIndicator">true</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:minHeight">11dp</item>
        <item name="android:maxHeight">11dp</item>
    </style>
    
    <style name="SwipeBackLayout">
        <item name="edge_size">50dip</item>
        <item name="shadow_left">@drawable/shadow_left</item>
        <item name="shadow_right">@drawable/shadow_right</item>
        <item name="shadow_bottom">@drawable/shadow_bottom</item>
    </style>

</resources>