<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:src="@drawable/loan_guide_four"
        android:scaleType="centerCrop" />

    <Button
        android:id="@+id/btnStart"
        android:layout_width="@dimen/loan_start_at_once_width"
        android:layout_height="@dimen/loan_start_at_once_height"
        android:layout_gravity="bottom|center_horizontal"
        android:text="立即开启"
        android:textColor="@color/loan_color_white"
        android:layout_marginBottom="@dimen/loan_start_at_once_margin_bottom"
        android:background="@drawable/loan_start_at_once_selector" />
</FrameLayout>